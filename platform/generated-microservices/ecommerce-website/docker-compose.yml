version: '3.9'

services:
  ecommerce-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecommerce-website
    ports:
      - "8094:8094"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8094
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=ecommerce-website
      - DB_USER=postgres
      - DB_PASSWORD=postgres
    volumes:
      - ./logs:/app/logs
      - ./target:/app/target
    depends_on:
      - postgres
    networks:
      - microservices
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8094/api/v1/ecommerce-website/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postgres:
    image: postgres:15-alpine
    container_name: postgres_ecommerce-website
    environment:
      POSTGRES_DB: ecommerce-website
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - microservices
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local

networks:
  microservices:
    driver: bridge
